# https://solar.skg.gg — WASM game loads at / (no separate landing page)
#
#   ./scripts/build-wasm.sh
#   npx wrangler pages deploy dist --project-name=solar

name = "solar"
compatibility_date = "2025-05-01"
pages_build_output_dir = "dist"
