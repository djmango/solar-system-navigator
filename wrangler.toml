# https://solar.skg.gg — React + Three.js mission planner at /
#
# Cloudflare Workers Builds (Connect to Git):
#   Build command:  bash scripts/cloudflare-pages-build.sh
#   Deploy command: npx wrangler deploy
#
# Worker name must match the Cloudflare dashboard project name exactly.

name = "solar-system-navigator"
compatibility_date = "2025-05-01"

[assets]
directory = "./dist"
