{
  "name": "solar-system-navigator",
  "private": true,
  "scripts": {
    "build": "bash scripts/cloudflare-pages-build.sh",
    "build:legacy": "bash -c 'unset NO_COLOR; trunk build --release --no-default-features --features web -p navigator-legacy'",
    "deploy": "wrangler deploy",
    "dev": "cd web && npm run dev",
    "dev:legacy": "bash -c 'compgen -G \"assets/textures/*.jpg\" >/dev/null || ./scripts/fetch_textures.sh; unset NO_COLOR; trunk serve --release --no-default-features --features web -p navigator-legacy'",
    "serve:dist": "npx --yes serve dist -p 8080"
  },
  "devDependencies": {
    "wrangler": "^4.24.0"
  }
}
