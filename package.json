{
  "name": "solar-system-navigator",
  "private": true,
  "scripts": {
    "build": "bash scripts/cloudflare-pages-build.sh",
    "deploy": "wrangler deploy",
    "compress-wasm": "node scripts/compress-wasm-brotli.mjs"
  },
  "devDependencies": {
    "brotli": "^1.3.3",
    "esbuild": "^0.25.12",
    "wrangler": "^4.24.0"
  }
}
