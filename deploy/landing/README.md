# Optional download mirror (not used for Cloudflare WASM deploy)

`solar.skg.gg` serves the **WASM game at `/`** via Trunk `dist/`.

Use this folder only if you self-host a **download-only** page (Proxmox + Caddy, or Pages with `deploy/landing` as output) and link to GitHub Releases for the Linux binary.
